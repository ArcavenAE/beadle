id: elem-propose-not-act
type: element
confidence: bedrock
title: "Propose-not-act for anything consequential or public; per-stage autonomy"
tags: [beadle, autonomy, guardrails, human-ai]
content: |
  Automation level is set per stage independently (acquire / analyze / decide /
  act — Parasuraman-Sheridan-Wickens). beadle reads and analyzes with high
  autonomy; it proposes for anything consequential or reader-facing. Bounded,
  reversible, additive actions (allow-listed labels, the dashboard rewrite) may
  run autonomously; free-text public comments default to a high-bar/review path
  with volume caps; closing/resolution is never autonomous (no stale-bot —
  information density is a protection signal). Every public output clears the same
  bar a careful human meets, or it is not posted (the curl AI-slop rule).
  Confidence is surfaced as frequency; evidence is cheap-to-verify links; every
  action is reversible with an audit trail and named-supervisor attribution.

  Comment-bar four lenses (finding-012). A comment clears the bar iff it fits
  one of: **fixed-pending-release** (cite commit + tests), **hallucinated-citation
  / fix-not-fixed** (cite the drift), **scope-drift** (cite the intent anchor),
  or **clear sibling cross-ref** (name the delta). *Opinion about the issue's
  worth, priority, or design merit fails the bar, no matter how correct.*
  *Disambiguation against cited artifacts or a verify-hint pointing at a
  60-second check the maintainer can run themselves passes.* Every draft is
  audited against three sharpening tests — verifiable-in-60s, names-a-specific-
  artifact, adds-a-pointer-the-maintainer-did-not-have — before it is posted.
  `arcavenai`'s own self-follow-ups on issues it filed are a *sibling signal*
  we notice, not a shape we imitate in beadle's voice.
edges:
  - target: grv-stale-autoclose
    type: supersedes
  - target: elem-maintainer-compass
    type: relates
provenance:
  created_by: agent
  created_at: "2026-06-27"
  updated_at: "2026-07-01"
