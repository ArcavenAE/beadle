id: question-intent-manifest-schema
type: question
confidence: frontier
title: "What is the minimal, versioned, machine-readable intent-manifest schema?"
tags: [beadle, intent, schema, frontier]
content: |
  Each target declares its intent so beadle can score against it
  (targets/<project>.intent.yaml). The v0 shape exists (goals, non_goals,
  scope_boundaries, alignment_rubric, maintainers, weighted intent_sources). Open:
  - How minimal can it be while still supporting a graded verdict? The GORE
    failure mode is a rich model nobody maintains.
  - For vsdd-factory the anchor is composite (CLAUDE.md + STATE.md + docs +
    git-history + maintainer comments + a future SOUL.md). How are weights combined?
  - How is the rubric kept honest as the project evolves — re-derive from sources
    each run, or pin and diff?
  - Held-out alignment scenarios (frontier F6 / StrongDM) — in-schema or separate?

  Concrete requirements surfaced by probes (the schema F1 was asking for, now
  partially answered and live in targets/vsdd-factory.intent.yaml at v0.3):
  - self_referential flag (finding-002) — inverts the "process-gap = drift" default
    for factories whose engine IS the product.
  - provenance_signal ranking axis (finding-002) — pilot-derived vs speculative markers.
  - explicit corpus-vs-per-issue scope for minutiae_signals (finding-002) — the
    led-by-the-backlog signal is a property of the filing pattern, not any one issue.
  - integrity_anchors (finding-004/005) — systems-of-record by recoverability tier
    (regenerable / spec_process / learning), so the silent-integrity classifier is
    grounded, not inferred.
edges:
  - target: elem-intent-alignment-whitespace
    type: derives
  - target: elem-silent-integrity-severity
    type: derives
  - target: elem-defect-classification-superset
    type: supports
provenance:
  created_by: agent
  created_at: "2026-06-27"
