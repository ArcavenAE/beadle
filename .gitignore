# beadle out-of-band state store (Phase 0 JSONL) and caches — never committed
.beadle/state/
.beadle/cache/
*.local

# Rust build output
/target/

# Local target-observation JSONL (live state, not source of truth)
/store/

# rendered docs
/tmp/beadle-*

# editor / OS
.DS_Store
*.swp
.idea/
.vscode/
.beads
